/**
 * banner-config.js
 * ---------------------------------------------------------------
 * Konfiguration für den schmalen Ankündigungs-Banner ganz oben auf
 * der Seite (z. B. für zeitlich begrenzte Aktionen).
 *
 * - visible: true/false — Banner ein- oder ausblenden.
 * - text: der angezeigte Satz. Kurz halten, läuft einzeilig.
 *
 * Sonst ist nichts anzupassen — Optik/Verhalten stecken in banner.js.
 * ---------------------------------------------------------------
 */
window.BANNER_CONFIG = {
  visible: true,
  text: "Nur bis Ende September: 10% auf alle Pakete für Neukunden"
};
